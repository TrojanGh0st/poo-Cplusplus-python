/*Programación estructurada: enfatiza la organización lógica y clara del código, y utiliza estructuras
de control de flujo como el if-then-else, while, for, entre otros. Un ejemplo en C++ sería:*/

#include <iostream>
using namespace std;

int main() {
    int x = 15; 

    if (x > 10) {
        cout << "x es mayor que 10" << endl;
    } else {
        cout << "x es menor o igual que 10" << endl;
    }

    return 0;
}
